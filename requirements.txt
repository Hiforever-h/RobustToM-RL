# GRPO/verl environment for Python 3.10, Linux and NVIDIA CUDA 12.1.
# RFT has a separate environment in rft/requirements.txt.
# Install the CUDA wheel first:
#   python -m pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu121
torch==2.4.0
# Versions supported by the bundled verl 0.1 vLLM adapter.
vllm==0.6.3
ray==2.10.0
xformers==0.0.27.post2
transformers>=4.45.2,<=4.46.0
tensordict>=0.5.0,<0.6
# FSDP model construction and Hydra configuration.
accelerate>=0.34.2,<1.0
hydra-core>=1.3.2,<1.4
omegaconf>=2.3.0,<2.4
codetiming>=1.4.0,<2.0
tqdm>=4.66.0,<5.0
# GRPO parquet construction/loading and runtime utilities.
numpy>=1.26.0,<2.0
pandas>=2.1.0,<3.0
pyarrow>=15.0.0,<18.0
safetensors>=0.4.3,<1.0
filelock>=3.13.0,<4.0
packaging>=23.2
ninja>=1.11.1,<2.0
# Remote experiment tracking. Version matches the bundled verl Docker image.
wandb==0.18.7
# Required by actor forward passes, but it must be installed separately after
# torch and this file because its build needs --no-build-isolation:
#   python -m pip install flash-attn==2.7.0.post2 --no-build-isolation
