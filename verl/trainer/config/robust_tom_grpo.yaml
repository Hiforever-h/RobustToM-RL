defaults:
  - ppo_trainer
  - _self_

data:
  train_files: data/grpo/counterfactual_process_reward_v3_fewshot/train.parquet
  val_files: data/grpo/counterfactual_process_reward_v3_fewshot/val.parquet
  prompt_key: prompt
  max_prompt_length: 2048
  max_response_length: 256
  train_batch_size: 8
  val_batch_size: 32
  apply_chat_template: true
  return_raw_chat: false
  seed: 2026

actor_rollout_ref:
  hybrid_engine: true
  use_trajectory_batch_sizes: true
  model:
    path: runs/final
    external_lib: null
    override_config: {}
    enable_gradient_checkpointing: true
    use_remove_padding: true
  actor:
    strategy: fsdp
    ppo_mini_batch_size: 128
    ppo_micro_batch_size: 2
    use_dynamic_bsz: false
    ppo_max_token_len_per_gpu: 4608
    grad_clip: 1.0
    clip_ratio: 0.2
    clip_ratio_low: 0.2
    clip_ratio_high: 0.3
    entropy_coeff: 0.001
    use_kl_loss: true
    kl_loss_coef: 0.001
    kl_loss_type: low_var_kl
    ppo_epochs: 1
    shuffle: false
    ulysses_sequence_parallel_size: 1
    optim:
      lr: 5.0e-7
      lr_warmup_steps_ratio: 0.0
      min_lr_ratio: null
      warmup_style: constant
      total_training_steps: -1
    fsdp_config:
      wrap_policy:
        min_num_params: 0
      param_offload: true
      grad_offload: true
      optimizer_offload: true
      fsdp_size: -1
  ref:
    model_path: runs/final
    fsdp_config:
      param_offload: true
      wrap_policy:
        min_num_params: 0
      fsdp_size: -1
    log_prob_micro_batch_size: 4
    log_prob_use_dynamic_bsz: false
    log_prob_max_token_len_per_gpu: 9216
    ulysses_sequence_parallel_size: 1
  rollout:
    name: vllm
    temperature: 1.0
    top_k: -1
    top_p: 1.0
    prompt_length: ${data.max_prompt_length}
    response_length: ${data.max_response_length}
    dtype: bfloat16
    gpu_memory_utilization: 0.35
    ignore_eos: false
    enforce_eager: true
    free_cache_engine: true
    load_format: dummy_dtensor
    tensor_model_parallel_size: 1
    max_num_batched_tokens: 32768
    max_num_seqs: 128
    log_prob_micro_batch_size: 4
    log_prob_use_dynamic_bsz: false
    log_prob_max_token_len_per_gpu: 9216
    do_sample: true
    n: 16
    seed: 2026

algorithm:
  gamma: 1.0
  lam: 1.0
  adv_estimator: grpo
  kl_penalty: kl
  kl_ctrl:
    type: fixed
    kl_coef: 0.001

trainer:
  total_epochs: 2
  total_training_steps: 800
  project_name: robust_tom_grpo_v3
  experiment_name: qwen25_3b_rft_grpo_n16_seed2026
  logger: [console, wandb]
  progress_bar: true
  nnodes: 1
  n_gpus_per_node: 1
  save_freq: 400
  test_freq: 50
  save_at_end: true
  validate_at_end: true
  val_before_train: true
  val_only: false
  critic_warmup: 0
  seed: 2026
  resume_from_path: null
  default_hdfs_dir: null
  default_local_dir: runs/grpo/${trainer.experiment_name}
