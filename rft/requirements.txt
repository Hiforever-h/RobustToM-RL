# Standalone RFT environment for Linux + NVIDIA CUDA 12.1.
# Recommended Python version: 3.10.
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.4.0
transformers>=4.45.2,<4.48
accelerate>=0.34.2,<1.0
vllm==0.6.3
numpy>=1.26,<2.0
safetensors>=0.4.3
tensorboard>=2.17,<3.0

# Optional but recommended on A800. Install separately because its build needs:
#   python -m pip install flash-attn==2.6.3 --no-build-isolation
